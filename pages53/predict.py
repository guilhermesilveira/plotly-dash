from dash import html, dcc
import plotly.graph_objs as go

# Contém o layout da página de gráficos
layout = html.Div([
    html.H1("Página de Predict"),
    # Demais componentes da página de gráficos
    # ...
])